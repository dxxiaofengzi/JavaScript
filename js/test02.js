// 内部引入JS文件,并输出换行符
document.write('外部引入JS文件' + "</br>");
//测试-假设小明数字成绩考试了80分，使用if...else语句判断考试成绩，是否及格(60分以上为及格)
var score = 80;
if (score < 60)
{
    Document.write('成绩不及格');
}else{
    document.write('成绩合格');
}

